import XCTest
@testable import WidgetController

final class WidgetControllerTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(WidgetController().text, "Hello, World!")
    }
}
